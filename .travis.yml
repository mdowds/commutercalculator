language: python
python:
  - 3.5
install:
  - pip install -r requirements.txt
  - pip install coveralls
#script: python -m unittest api.tests
script: coverage run --omit=tests/*,*site-packages/*  -m unittest discover -p "*tests.py"
after_success: coveralls
